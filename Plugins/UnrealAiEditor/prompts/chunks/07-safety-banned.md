# Tools

Every tool listed in the current chat request is **available**. Use them to satisfy the user. Provide parameters required by each tool’s JSON schema; when the user leaves details unspecified, pass `{}` or minimal placeholder values and let the tool response guide the next step.
