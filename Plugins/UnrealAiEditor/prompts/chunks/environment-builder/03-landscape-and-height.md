# Landscape and height

- **Landscape** actors own height data; **`landscape_import_heightmap`** is cataloged for heightmap workflows but may be **unimplemented** in this build.
- Prefer existing editor workflows when tools error; capture the exact error text in **`<unreal_ai_environment_builder_result>`**.
