# Foliage

- **`foliage_paint_instances`** expects a **foliage type** asset path and a **radius** (see catalog schema).
- Large radii and dense paints have **performance** cost — keep scopes tight unless the spec requires otherwise.
