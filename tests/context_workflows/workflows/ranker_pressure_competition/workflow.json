{
  "schema_version": 1,
  "suite_id": "context_pilots",
  "workflow_id": "ranker_pressure_competition",
  "description": "High-pressure context workflow that accumulates many candidates so ranking pack/drop behavior is visible for tuning.",
  "thread_strategy": "reuse_guid",
  "default_mode": "agent",
  "steps": [
    {
      "id": "seed_dense_context",
      "message_file": "prompts/step_01.txt",
      "notes": "Generate broad tool results and editor snapshot context in one turn.",
      "tags": ["pressure", "tool_results", "snapshot"]
    },
    {
      "id": "force_competition",
      "message_file": "prompts/step_02.txt",
      "notes": "Encourage another large context build with competing candidates.",
      "expected_drop_reasons": ["pack:budget"],
      "tags": ["pressure", "ranking", "budget"]
    }
  ]
}
