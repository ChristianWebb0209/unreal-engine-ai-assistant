{
  "schema_version": 1,
  "suite_id": "context_pilots",
  "workflow_id": "conv_memory_smoke",
  "description": "Two turns on one thread: store a short fact, then ask the model to recall it. Observes conversation + tool memory in built context.",
  "thread_strategy": "reuse_guid",
  "default_mode": "agent",
  "steps": [
    {
      "id": "remember_codeword",
      "message_file": "prompts/step_01.txt",
      "notes": "User establishes a codeword for later recall.",
      "tags": ["conversation", "memory"]
    },
    {
      "id": "recall_codeword",
      "message_file": "prompts/step_02.txt",
      "expect_context_hints": "Prior user message and any tool results from step 1 should influence the reply.",
      "tags": ["conversation", "memory"]
    }
  ]
}
