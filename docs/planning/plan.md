Read through tooling-problem.md. treat this as the main task. our goal is to refactor the entire tools schema, resolvers, and implementation to follow this as a guideline. currently, despite weeks of iteration, our tools basically can't do anything, since the llm can't utilize them properly for the reasons listed in tooling-problem.md. Your job is to rebuild them, basically from scratch. Rebuild their implementations, resolvers, schemas, and everything. Note how we have built our tool picker already, so we send the top k tools by relevance to context. You may have to update this system slightly, or at least add very good tags to all the tools you make, since there will likely be a lot of tools.